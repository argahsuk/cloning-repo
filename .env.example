# MongoDB connection string (MongoDB Atlas)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/civicbridge?retryWrites=true&w=majority

# Session secret for signing (use a long random string in production)
SESSION_SECRET=your-session-secret-at-least-32-chars
